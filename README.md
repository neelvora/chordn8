# chordn8

A React Native app I started in February 2024 and never took past setup. Every file here is what `@react-native-community/cli` generated: `App.tsx` is still the sample screen with the Step One and Debug sections, and the only dependencies are `react` and `react-native` 0.73.4.

The repo description calls it a tool for musicians. None of that is written yet.
